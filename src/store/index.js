import {
    reducer as albumsReducer,
    types as albumsTypes,
    initialState as albumsInitialState
} from './albumsReducer'
import {
    reducer as thumbnailReducer,
    types as thumbnailTypes,
    initialState as thumbnailInitialState
} from './thumbnailReducer'

export {
    albumsReducer,
    albumsTypes,
    albumsInitialState,
    thumbnailReducer,
    thumbnailTypes,
    thumbnailInitialState
}
